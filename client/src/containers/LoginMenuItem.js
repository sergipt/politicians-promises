import React from 'react';
import '../App.css';

class LoginMenuItem extends React.Component {
  state = {}

  render () {
    if () {
      return (
        <div className="AddPromise">
          <Menu.Item name='login' active={activeItem === 'login'} onClick={this.handleLoginModalOpen} />
        </div>
      )
    } else {
      return (
        <div className="AddPromise">
          <Menu.Item name='login' active={activeItem === 'login'} onClick={this.handleLoginModalOpen} />
        </div>
      )
    }
  }
}


export default LoginMenuItem;