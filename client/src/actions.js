export const addLists = (lists) => ({
  type: 'ADD_LISTS', lists
});

export const addPromises = (promises) => ({
  type: 'ADD_PROMISES', promises
});

export const addMyVotes = (votes) => ({
  type: 'ADD_MY_VOTES', votes
});

export const setUser = (user) => ({
  type: 'SET_USER', user
});

// export const addLists = (lists) => ({
//   type: 'ADD_LISTS', lists
// });

// export const addPromise = (promise) => ({
//   type: 'ADD_PROMISE', promise
// });